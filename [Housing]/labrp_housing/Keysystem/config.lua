-- Config = {
--     EnableESXIdentity = true, -- use firstname lastname format instead of steam / fivem names when giving keys
--     Command = "keyss", -- command to open the menu
-- }

-- Strings = {

-- }